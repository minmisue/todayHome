<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="housePictureBoard">
	<insert id="createHousePictureBoard" parameterType="com.sp.app.board.HousePictureBoard">
		INSERT INTO USER_BOARD(user_board_id, member_id, board_type_id, created_date, 
			subject, hitCount, ip_address)
		VALUES (seq_user_board.NEXTVAL, #{memberId}, 1, SYSDATE,
			#{subject}, 0, 111.222.333.444)
	</insert>
</mapper>